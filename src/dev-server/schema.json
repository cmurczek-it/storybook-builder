{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "properties": {
    "ci": {
      "type":"boolean",
      "description": "CI mode (skip interactive prompts, don't open browser)",
      "default": false
    },
    "configDir": {
      "type":"string",
      "description": "Directory where to load Storybook configurations from",
      "default": ".storybook"
    },
    "host": {
      "type": "string",
      "description": "Host to run Storybook",
      "default": "localhost"
    },
    "sslCa": {
      "type": "string",
      "description": "Provide an SSL certificate authority. (Optional, but required if using a self-signed certificate)"
    },
    "sslCert": {
      "type": "string",
      "description": "Provide an SSL certificate to use for https."
    },
    "sslKey": {
      "type": "string",
      "description": "Provide an SSL key of the ssl-cert."
    },
    "port": {
      "type": "number",
      "description": "Port to run Storybook",
      "default": 4400
    },
    "staticDirs": {
      "type": "string",
      "description": "Directory where to load static files from, comma-separated list"
    }
  }
}
