{
  "$schema": "http://json-schema.org/schema",
  "$id": "StorybookBuilderAddSchematics",
  "title": "Storybook-Builder Add Schema",
  "type": "object",
  "properties": {
    "project": {
      "description": "The project to which to add storybook. Defaults to the default project of the workspace",
      "type": "string"
    }
  }
}
